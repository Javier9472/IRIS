# .env.example — versionado, solo plantilla, sin valores reales

# Generar con:
# python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=

DEBUG=False

# Dominio del túnel (ngrok/Cloudflare Tunnel), sin protocolo. Vacío en local.
TUNNEL_DOMAIN=